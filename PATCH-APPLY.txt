Overlay this archive on top of the MJT 3.0.0-SNAPSHOT+8 source tree.

It changes Main, CommandContext, CommandDispatcher, PanelService, StateStore and BuildInfo.
It adds src/main/java/mjt/services/workspace/.

Build:
  mvn -U clean package

Smoke tests:
  .mjt workspace list
  .mjt workspace show server-root
  .mjt workspace sync
  GET /api/capabilities
  GET /api/workspaces
